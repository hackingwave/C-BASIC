#include<stdio.h>
int main(){
       int n;
       scanf("%d",&n);
       int sum=0;
       for(int i=1;i<=n;i++ )
       {
              
              sum=sum+(((i*(i-1))/2 +1)*((i*(i-1))/2 +1)*((i*(i-1))/2 +1)*((i*(i-1))/2 +1));
       }
       
       
       printf("%d",sum);
       
       
       
       
       
       return 0;
}
